/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function (haystack, needle) {
  //find the occurence of the word and output the index at which it occurs

  //if it does not occur output -1

  return haystack.indexOf(needle);
};
